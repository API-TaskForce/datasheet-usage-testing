global:
  scrape_interval: 5s # Frecuencia alta para ver los tests en tiempo real

scrape_configs:
  - job_name: 'api-limiter-backend'
    static_configs:
      - targets: ['host.docker.internal:3000'] # Apunta a tu servidor Express